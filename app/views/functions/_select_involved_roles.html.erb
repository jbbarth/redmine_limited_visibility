<p id=involved-roles-form class=involved-roles>
  <label><%= l(:label_involved_members) %></label>
  <% Function.available_functions_for(@project).sorted.each do |function|
      role_is_involved = viewers.include?(function.id)
      role_is_mine = current_user_visibility_roles.include?(function)
    %>
    <% styles = role_is_involved ? 'involved' : '' %>
    <% styles << (role_is_mine ? ' mine' : '') %>
    <%= render "functions/visibility_role", :function => function, :styles => styles %>
  <% end %>
</p>

<% if @project.present? && !User.current.allowed_to?(:use_issues_visibility_filter, @project) %>
  <%= javascript_tag do %>
    $(document).ready(function(){
        $('#tr_authorized_viewers').css('display', 'none');
        $('select#operators_authorized_viewers').val('mine');
    });
  <% end %>
<% end %>

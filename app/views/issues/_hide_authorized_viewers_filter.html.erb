<% if @project.present? && !User.current.allowed_to?(:can_see_issues_visibility, @project) %>
  <%= javascript_tag do %>
    $(document).ready(function(){
        $('#tr_authorized_viewers').css('display', 'none');
        $('select#operators_authorized_viewers').val('mine');
    });
  <% end %>
<% end %>
